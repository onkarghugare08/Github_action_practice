name: Scheduled Workflow

on:
  schedule:
    - cron: "* * * * *"

jobs:
  scheduled-job:
    runs-on: ubuntu-latest

    steps:
      - name: Show date
        run: date

      - name: Show message
        run: echo "Scheduled workflow is running!"
